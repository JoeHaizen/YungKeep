🧠 Exemple simple
Tu as une API Spring Boot qui gère l’authentification.

Tu as un site React qui permet de se connecter.

Le client, c’est React.

Quand l’utilisateur se connecte :

React envoie la requête LoginRequest à l’API.

L’API répond avec un JwtResponse.

React stocke le token en localStorage.

À chaque requête future, React ajoute le token dans les en-têtes pour prouver que l’utilisateur est connecté.